# PRD Updated: Current vs. New Features Matrix
## What Was Added to Help Claude Code Plan

---

## 📋 What's New in the PRD

The **MOOD_BOARD_PRD_PHASES_1_6.md** file now includes a comprehensive planning section right after the executive summary:

### 1. **CURRENT VS. NEW FEATURES MATRIX**

Three detailed tables showing:

#### Table 1: What Stays (Fully Retained)
- Lists 7 features that are KEPT from the current system
- Shows which component uses it
- Explains WHY it should stay
- Example: "Section selection & editing" → Keep because "Still valuable for user control"

#### Table 2: What Gets Replaced (Steps 1-5 & Step 8)
- Lists 7 features that are REMOVED/REPLACED
- Shows what currently does it
- Shows what replaces it
- Shows the new component
- Explains the rationale
- Example: "12-preset picker" → Replaced by "3 AI-generated moods" because "Contextually relevant, not generic"

#### Table 3: New Features (Additions)
- Lists 7 brand new features being added
- Shows which phase they appear in
- Shows the new component/logic
- Shows effort hours
- Example: "Mood generation engine" → 10 hours in Phase 1

---

### 2. **STEP-BY-STEP MIGRATION MAP**

Visual flowcharts showing:

#### Current Flow (9 Steps)
```
Step 1: Project Name        → Skip (merge into home)
Step 2: Design Direction    → REPLACE with mood selection
Step 3: Inspiration Images  → AUTO-POPULATE from mood
...
```

#### New Flow (6 Visible Steps, 8 Total Internal)
```
Step 0: Home Input           → (new) User describes project
Step 1: Mood Selection       → (replaces 1-5)
...
```

**Why This Helps Claude Code:**
- Shows exactly which steps disappear
- Shows which steps get renamed
- Shows the new flow clearly
- Makes it obvious what to tackle first

---

### 3. **IMPLEMENTATION IMPACT GUIDE**

Detailed breakdown of file changes:

#### Files to CREATE (7 New Components, 5 New Hooks, 3 New Exporters)
```
src/components/
  ├── MoodSelection.tsx
  ├── MoodCard.tsx
  ├── LoadingMoodProgress.tsx
  └── steps/ (4 new step components)

src/hooks/ (5 new hooks)
src/utils/exporters/ (3 new export generators)
```

#### Files to MODIFY (8 Existing Files)
- App.tsx (change step routing)
- ProjectContext.tsx (add state fields)
- types.ts (add new types)
- ProgressBar.tsx (update progress bar)
- Rename 5 existing step components

#### Files to DELETE (7 Components)
- Old Step1-5 components
- Old Step7-8 components

#### Files to KEEP UNCHANGED (9 Files)
- StepNav.tsx, TeachingTooltip.tsx, etc.
- useProjectApi.ts (no changes)
- All utility functions

**Why This Helps Claude Code:**
- Crystal clear what stays vs. goes
- Knows exactly which files to touch
- Can plan refactoring order
- Won't miss deleting old files

---

### 4. **STATE SHAPE EVOLUTION**

Shows exactly how the WizardState changes:

#### Current WizardState
```typescript
{
  projectName: string;
  industry: string;
  businessType: string;
  presetId: string;
  // ... 6 more fields
}
```

#### New WizardState (Extended)
```typescript
{
  // KEEP: Everything above, plus...
  
  // NEW (Phase 1-2)
  projectInput?: string;
  generatedMoods?: Mood[];
  selectedMood?: Mood;
  // ... 5 more new fields
  
  // DEPRECATED (no longer used)
  // - businessType (derived from mood)
  // - inspirationImages (auto-fetched)
}
```

**Why This Helps:**
- Exactly what fields to add to ProjectContext
- What fields can be deprecated
- Prevents state shape conflicts

---

### 5. **IMPLEMENTATION PLANNING GUIDE** ⭐ KEY ADDITION

This section tells Claude Code exactly how to build this project:

#### Recommended Build Order (20-Day Sprint)

**Phase 0: Setup (Day 1, 3 hours)**
- Update App.tsx routing
- Update ProjectContext
- Update types.ts

**Phase 1: Mood Selection (Days 2-3, 12 hours)**
1. Create MoodSelection.tsx
2. Create MoodCard.tsx
3. Create useMoodGeneration hook
4. Add API endpoint
5. Wire into App.tsx
6. Test

**Phase 1b: Clean Up (Days 6-7, 6 hours)**
- Delete old components (AFTER Phase 1-2 work)

**Phase 2: Auto-Population (Days 4-5, 12 hours)**
- Create keyword extraction
- Create section generation
- Create multi-source images
- Add API endpoints
- Test

**Phases 3-7: Continuing pattern...**
- Brand kit (Days 8-9)
- Section mockups (Days 10-11)
- Element refinement (Days 12-13)
- Export (Days 14-15)
- Testing (Days 16-18)
- Deploy (Days 19-20)

**Why This Helps:**
- Clear day-by-day plan
- Knows what to prioritize
- Can track progress
- Knows when to delete old code (not too early!)
- Knows what to test at each step

---

### 6. **CHECKPOINT VERIFICATION LISTS**

For each phase, a checklist of what must work before moving on:

#### After Phase 1 (Mood Selection)
- [ ] MoodSelection component renders
- [ ] 3 mood cards generated from Claude API
- [ ] Mood card JSON is valid
- [ ] Selecting a mood triggers correct action
- [ ] Response time <3 seconds
- [ ] Error handling works
- [ ] Loading state appears

#### After Phase 2 (Auto-Population)
- [ ] Keywords extracted
- [ ] Colors set correctly
- [ ] Section queries are mood-specific
- [ ] Images fetched in parallel
- [ ] Auto-population <2 seconds total
- [ ] Jump to Step 6 works

**Why This Helps:**
- Prevents moving forward with broken code
- Forces testing at each milestone
- Clear go/no-go decision points

---

### 7. **KNOWN CHALLENGES & PREPARATION**

Warns Claude Code about 4 likely issues:

1. **Claude API Latency** (3-5 seconds is normal)
2. **Image Query Relevance** (mood-specific queries needed)
3. **Color Harmony Validation** (check WCAG contrast)
4. **State Complexity** (don't over-engineer)
5. **Mobile Responsiveness** (test at 375px early)

**Why This Helps:**
- Prevents frustration with "slow" API calls
- Explains why generic queries fail
- Guides toward validation approach
- Sets realistic expectations

---

### 8. **ROLLBACK PLAN**

If something breaks:

1. Revert App.tsx (go back to step 1-9 routing)
2. Keep old Step1-5 components (don't delete yet)
3. Disable new MoodSelection route
4. Test old flow still works
5. Debug in isolation
6. Re-integrate when fixed

**Why This Helps:**
- Safe experimentation
- Can recover from mistakes quickly
- Gives confidence to refactor

---

### 9. **SIGN-OFF CRITERIA**

Final checklist before marking the project "done":

- [ ] All 6 phases build without errors
- [ ] Full user flow works end-to-end
- [ ] No TypeScript errors
- [ ] <5 second response times
- [ ] Mobile responsive (375px)
- [ ] WCAG AA accessibility
- [ ] User-friendly error messages
- [ ] Old components removed
- [ ] Database migrations ready

---

## 🎯 How This Helps Claude Code

### Before (Without these additions):
- Claude reads 2,000-line PRD
- Confused about what to keep vs. delete
- Not sure of build order
- Wastes time figuring out what to prioritize
- No checkpoints for validation
- Might break existing code

### After (With these additions):
- **Day-by-day plan**: Know exactly what to build each day
- **Clear scope**: See exactly what stays, changes, deletes
- **Build order**: Start with Phase 1, don't jump ahead
- **Checkpoints**: Know when to test, what must work
- **Risk mitigation**: Understand likely issues upfront
- **Rollback plan**: Can recover from mistakes
- **Sign-off criteria**: Know when it's actually done

---

## 📊 What The Tables Tell Claude Code

### Table 1 (What Stays)
"Don't break these when you refactor. They're valuable and worth preserving."

### Table 2 (What Replaces)
"This old feature becomes this new feature. Here's why it's better."

### Table 3 (New Features)
"We're adding 7 brand new things. Here's how much effort each takes."

### Build Order
"Don't do everything at once. Do it in this specific order. Stop and test after each phase."

### Checkpoints
"Don't move forward until these 7 things pass. If they don't, debug first."

### Known Challenges
"You'll probably hit these 5 issues. Here's what to expect and how to handle it."

---

## 🚀 Result

Claude Code now has everything needed to:

✅ Understand the full scope (what stays, changes, goes)  
✅ Plan the implementation (20-day sprint with phases)  
✅ Build in the right order (no dependencies missed)  
✅ Test at each step (prevent integration disasters)  
✅ Know when it's done (clear sign-off criteria)  
✅ Prepare for problems (known challenges section)  
✅ Recover from mistakes (rollback plan)  

This transforms the PRD from "what to build" to "exactly how to build it day-by-day."

